services:
  app:
    container_name: fastapi
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    # environment:
    volumes:
      - .:/src
    command: ["uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "8000"]